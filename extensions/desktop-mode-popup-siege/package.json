{
	"name": "desktop-mode-popup-siege",
	"version": "0.1.0",
	"private": true,
	"description": "Popup Siege companion game for OpenStation.",
	"scripts": {
		"build": "node scripts/build-runtime.mjs",
		"check": "npm run build && node scripts/verify-release.mjs",
		"test": "node --test tests/*.test.js && php tests/registration-smoke.php"
	}
}
