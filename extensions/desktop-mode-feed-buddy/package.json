{
	"name": "desktop-mode-feed-buddy",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "vite build --mode development && vite build --mode production",
		"lint": "eslint \"src/**/*.ts\"",
		"typecheck": "tsc --noEmit",
		"test": "vitest run --config vitest.config.ts"
	}
}
